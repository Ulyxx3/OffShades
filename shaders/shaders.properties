# OffShades — shaders.properties
# Iris/OptiFine shader configuration
# Reference: https://github.com/IrisShaders/Iris/blob/main/docs/iris_rendering_pipeline.md

# ─── Shadow Map ────────────────────────────────────────────────
# Will be activated in Step 2 (directional shadows)
# shadow.enabled=true
# shadowMapResolution=2048
# shadowDistance=96.0

# ─── Render Targets (color buffers) ────────────────────────────
# colortex0 : albedo (base color)         — rgba8
# colortex1 : normals + material data     — rgb10_a2  (future use)
# colortex2 : specular / PBR data         — rgba8     (future use)

# ─── Screen-Space Effects ──────────────────────────────────────
# Antialiasing will be added later (TAA or FXAA)
